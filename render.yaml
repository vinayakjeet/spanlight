# Render free tier. The service spins down when idle, so the first request after
# a quiet period pays a cold start. That is accepted and instrumented rather
# than engineered around: the first span after a spin-up carries
# `spanlight.cold_start` so the dashboards annotate the outlier instead of
# hiding it.
services:
  - type: web
    name: spanlight
    runtime: python
    plan: free
    # `--extra app` because the demo's web dependencies live outside
    # `[project].dependencies`, which holds only what the library itself needs.
    # Without it the container installs Spanlight and no web server.
    buildCommand: pip install uv && uv sync --frozen --no-dev --extra app
    # Call the interpreter the build produced rather than going through `uv run`.
    # `uv run` re-resolves and syncs before executing, which on a cold container
    # blows past Render's port-scan window, and it assumes uv is on the runtime
    # PATH, which is not guaranteed just because the build step installed it.
    startCommand: .venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /healthz
    envVars:
      - key: PYTHON_VERSION
        value: "3.13.2"
      - key: ENVIRONMENT
        value: production
      # Set in the Render dashboard, never committed. Unset until M0.1, which
      # leaves spanlight.init() a deliberate no-op rather than a silent failure.
      - key: OTEL_EXPORTER_OTLP_ENDPOINT
        sync: false
      - key: OTEL_EXPORTER_OTLP_HEADERS
        sync: false
